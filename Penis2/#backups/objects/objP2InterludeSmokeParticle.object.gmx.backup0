// 2024-01-28 17:54:53
#event properties (no comments/etc. here are saved)
parent_index = -1;
uses_physics = false;
sprite_index = sprP2InterludeSmokeParticle;

#event create
gravity = 0.001;
gravity_direction = 90;
direction = random_range(0, 80);
speed = random_range(0.05, 0.08);
alarm[0] = random_range(250, 350);
image_speed = 0.05;
image_index = irandom(image_number - 1);
depth = -2000000;

#event alarm0
instance_destroy();