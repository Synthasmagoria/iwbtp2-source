// 2024-06-30 16:10:19
#event properties (no comments/etc. here are saved)
parent_index = -1;
uses_physics = false;

#event create
not_in_tile_area_callback = -1;

#event step
with (objPlayer) {
    if (!place_meeting(x, y, oP2TileArea) && script_exists(other.not_in_tile_area_callback)) {
        script_execute(other.not_in_tile_area_callback);
        instance_destroy();
    }
}
